{% extends 'admin/index.html.twig' %}

{% block admin_content %}
<h3 class="text-center"><i class="fas fa-chart-pie"></i> Panel de Control</h3>

<div class="container mt-5">
    <!-- 🔹 Mensajes Flash -->
    {% for type, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ type }} fade show">
                <i class="fas fa-exclamation-circle"></i> {{ message }}
            </div>
        {% endfor %}
    {% endfor %}

    <!-- 🔹 Tarjetas de métricas centradas -->
    <div class="row justify-content-center">
        <div class="col-md-3">
            <div class="card shadow text-center">
                <div class="card-body">
                    <h5 class="card-title"><i class="fas fa-users"></i> Usuarios Registrados</h5>
                    <p class="fs-3 fw-bold text-primary">{{ totalUsuarios }}</p>
                </div>
            </div>
        </div>

        <div class="col-md-3">
            <div class="card shadow text-center">
                <div class="card-body">
                    <h5 class="card-title"><i class="fas fa-box-open"></i> Productos Activos</h5>
                    <p class="fs-3 fw-bold text-success">{{ totalProductos }}</p>
                </div>
            </div>
        </div>

        <div class="col-md-3">
            <div class="card shadow text-center">
                <div class="card-body">
                    <h5 class="card-title"><i class="fas fa-shopping-bag"></i> Ventas Totales</h5>
                    <p class="fs-3 fw-bold text-danger">{{ totalVentas }}€</p>
                </div>
            </div>
        </div>
    </div>

    <!-- 🔹 Accesos rápidos como tarjetas horizontales -->
    <div class="row mt-4">
        <div class="col-md-4">
            <a href="{{ path('admin_users') }}" class="card shadow text-center text-decoration-none">
                <div class="card-body">
                    <h5 class="card-title"><i class="fas fa-user-cog"></i> Gestión de Usuarios</h5>
                </div>
            </a>
        </div>
        <div class="col-md-4">
            <a href="{{ path('admin_categories') }}" class="card shadow text-center text-decoration-none">
                <div class="card-body">
                    <h5 class="card-title"><i class="fas fa-folder-open"></i> Gestión de Categorías</h5>
                </div>
            </a>
        </div>
        <div class="col-md-4">
            <a href="{{ path('admin_products') }}" class="card shadow text-center text-decoration-none">
                <div class="card-body">
                    <h5 class="card-title"><i class="fas fa-boxes"></i> Gestión de Productos</h5>
                </div>
            </a>
        </div>
    </div>
</div>
{% endblock %}
